
# Write a program to take an integer as an input and check whether it ends with 4 or 8. If its ends with 4, print “ends with 4 “, if it ends with 8, print “ends with 8”, otherwise print “ ends with neither ” .

a= int(input("Enter a number: "))
print(f"{a} ends with: ",a%10 if a%10 in [4,8] else print(f"{a} doesn't end with 8 or 4"))
