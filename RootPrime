# -*- coding: utf-8 -*-

#Check if root of a number is prime or not

from math import sqrt as sqrt
def prime(n):
    for i in range(2, (n//2)+1):
        if int(n)%i == 0:
            return False
    else:
        return True
print(prime(int(sqrt(int(input("Enter a number: "))))))
